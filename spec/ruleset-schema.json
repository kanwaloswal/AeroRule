{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AeroRule RuleSet",
  "description": "Defines a group of rules to be evaluated together with a specific execution strategy",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier for the ruleset"
    },
    "name": {
      "type": "string",
      "description": "Short, human-readable name of the ruleset"
    },
    "description": {
      "type": "string",
      "description": "Human-readable description of the ruleset's purpose"
    },
    "executionStrategy": {
      "type": "string",
      "enum": ["ALL", "FIRST_MATCH", "PRIORITY_ORDERED", "GATED", "FLOW"],
      "default": "ALL",
      "description": "ALL = evaluate every rule. FIRST_MATCH = stop at first match. PRIORITY_ORDERED = sort by priority descending, run all. GATED = stop on first failure. FLOW = branch by following next pointers."
    },
    "rules": {
      "type": "array",
      "items": { "$ref": "rule-schema.json" },
      "description": "Ordered list of rules to evaluate"
    }
  },
  "required": ["id", "rules"]
}
